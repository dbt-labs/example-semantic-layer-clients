[project]
name = "afs-example"
version = "0.1.0"
description = "Example for using ADBC to query Semantic Layer."
python = ">=3.9"

dependencies = [
  "adbc-driver-flightsql>=0.5.1,<1.0",
  "pandas"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
